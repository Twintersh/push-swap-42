{
    "tasks": [
		{
			"label": "DEBUG",
			"command": "make",
            "args": ["debug"],
			"type": "shell",
			"options": {
                "cwd": "${fileDirname}"
			}
		},
    ],
    "version": "2.0.0"
}